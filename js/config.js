/* =========================================================================
   EasyLead. Единственный файл, который нужно отредактировать перед запуском.
   ========================================================================= */

window.EASYLEAD = {

  /* Куда уходит заявка.
     Пустая строка = форма не подключена: посетителю будет показано
     сообщение с телефоном и Telegram, ложного «отправлено» не будет.

     Готовый обработчик лежит в worker/telegram-lead.js
     Инструкция по установке: README.md, раздел «Подключение формы». */
  endpoint: '',

  /* Контакты. Подставляются во все ссылки на странице. */
  phone:     '+7 000 000-00-00',
  phoneHref: '+70000000000',
  telegram:  'https://t.me/easylead',

  /* Куда вести после успешной отправки. Пусто = остаёмся на странице. */
  thanksUrl: ''
};

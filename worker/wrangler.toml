name = "gbo-avto-leads"
main = "src/worker.js"
compatibility_date = "2026-08-01"

# Антиспам-хранилище (rate limit по IP). Создать: wrangler kv namespace create LEADS_RL
# [[kv_namespaces]]
# binding = "LEADS_RL"
# id = "<kv_namespace_id>"

[vars]
# Домены, с которых разрешено принимать заявки. Через запятую, без слэша на конце.
ALLOWED_ORIGINS = "https://gbo-avto.ru,https://www.gbo-avto.ru,https://gbozlat.host-ai.site,http://localhost:8080"

# Секреты (НЕ хранить в этом файле):
#   wrangler secret put TG_BOT_TOKEN
#   wrangler secret put TG_CHAT_ID

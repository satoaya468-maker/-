name = "gbo-avto-leads"
main = "src/worker.js"
compatibility_date = "2026-08-01"

# Антиспам-хранилище (rate limit по IP). Создать: wrangler kv namespace create LEADS_RL
# [[kv_namespaces]]
# binding = "LEADS_RL"
# id = "<kv_namespace_id>"

[vars]
# Домены, с которых разрешено принимать заявки. Через запятую, без слэша на конце.
# гбо-автосервис.рф записан в punycode: браузер шлёт Origin именно так,
# на кириллический вид проверка не сработает и заявки получат 403.
# host-ai оставлен на время переезда, localhost — для локальной сборки.
ALLOWED_ORIGINS = "https://xn----8sbbgabsz2cdxkeh.xn--p1ai,https://www.xn----8sbbgabsz2cdxkeh.xn--p1ai,https://gbozlat.host-ai.site,http://localhost:8080"

# Секреты (НЕ хранить в этом файле):
#   wrangler secret put TG_BOT_TOKEN
#   wrangler secret put TG_CHAT_ID

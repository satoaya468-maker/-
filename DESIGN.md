# Дизайн-система EasyLead

Регистр: brand. Референсная полоса: Linear / Vercel / Resend. Тёмный
монохром, крупная типографика, глубина за счёт слоёв и света, ноль цвета.

## Цвет

Монохром без насыщенности. Белый и есть акцент. Ни одного цветного элемента,
включая состояния ошибки и успеха (они различаются иконкой и текстом, не цветом).

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#08090B` | фон страницы (не чистый `#000`, чтобы слои читались) |
| `--bg-lift` | `#0C0E12` | чередующиеся секции |
| `--surface` | `rgba(255,255,255,.025)` | поверхность первого уровня |
| `--surface-2` | `rgba(255,255,255,.05)` | поверхность второго уровня |
| `--line` | `rgba(255,255,255,.09)` | волосяная граница |
| `--line-2` | `rgba(255,255,255,.18)` | граница в фокусе и на ховере |
| `--text` | `#F4F5F6` | основной текст |
| `--muted` | `#A2A5AB` | вторичный текст, контраст 8.0:1 |
| `--dim` | `#7A7E85` | подписи и placeholder, контраст 4.8:1 |

Все три уровня текста проверены на контраст против `--bg`: 18.9:1, 8.0:1, 4.8:1.
Порог WCAG AA (4.5:1) выдержан даже для самого тусклого.

## Глубина

На фоне `#08090B` обычная тень не видна, поэтому уровень задаётся тремя
признаками сразу: собственным фоном, светлотой границы и внутренней
подсветкой сверху. Тень только дополняет верхний уровень, самостоятельным
источником глубины она не работает.

| Уровень | Фон | Граница | Тень |
|---|---|---|---|
| страница | `--bg` `#08090B` | нет | нет |
| утопленное поле | `--sunken-bg` `#0B0C0E` | `--card-line` | нет |
| карточка | `--card-bg` `#101113` | `--card-line` `rgba(255,255,255,.07)` | `inset 0 1px 0 rgba(255,255,255,.05)` |
| приподнятая карточка | `--raised-bg` `#17191C` | `--raised-line` `rgba(255,255,255,.11)` | `inset 0 1px 0 rgba(255,255,255,.07)`, `0 24px 48px -12px rgba(0,0,0,.7)` |

На наведении усиливается **граница** до `--line-hover`
`rgba(255,255,255,.18)`, а не тень: рост тени на тёмном фоне глазом не
читается.

Раскладка по уровням: форма заявки приподнятая (это цель страницы), панель
заявок и блок услуг обычные карточки, поля ввода утоплены внутри формы.
Панель переходит на приподнятый уровень в состоянии «доставлено».

Плюс три источника фактуры: зерно (`--grain`, фиксированный слой),
радиальное свечение за героем, направленный свет над секцией-заявлением.

## Типографика

Одно семейство, контраст весом. Моно только там, где на экране машинный вывод:
цифры цен и панель заявок.

- дисплей и интерфейс: **Onest** 300 / 500 / 700 (кириллица, вариативный)
- машинный вывод: **JetBrains Mono** 400 / 500

Шкала, шаг >= 1.25, вся на `clamp()`:

| Ступень | Размер | Трекинг | Интерлиньяж |
|---|---|---|---|
| `--t-hero` | `clamp(2.35rem, 6.2vw, 4.35rem)` | `-.04em` | `1.06` |
| `--t-h2` | `clamp(1.9rem, 4.6vw, 3.1rem)` | `-.04em` | `1.12` |
| `--t-h3` | `clamp(1.15rem, 2.2vw, 1.45rem)` | `-.02em` | `1.3` |
| `--t-body` | `clamp(1rem, .4vw + .9rem, 1.125rem)` | `-.011em` | `1.65` |
| `--t-sm` | `.9375rem` | `0` | `1.55` |
| `--t-xs` | `.8125rem` | `0` | `1.5` |

Трекинг зависит от кегля: чем крупнее, тем плотнее. Мелкие подписи набираются
без трекинга. `font-variant-numeric: tabular-nums` включён на `body`, поэтому
цены, телефоны и время в панели выравниваются по разрядам.

Светлый текст на тёмном читается легче, поэтому интерлиньяж на 0.05 больше,
чем взяли бы для тёмного текста на светлом. Длина строки ограничена 62ch.

## Форма

| Токен | Значение | Где |
|---|---|---|
| `--r-lg` | 16px | карточки, панели, формы |
| `--r-md` | 10px | кнопки, поля ввода, вложенные блоки |
| `--r-sm` | 6px | метки, чипы, рамка фокуса |

Вложенный радиус всегда меньше внешнего. Одинакового радиуса у элементов,
вложенных друг в друга, на странице нет: форма 16, поле внутри неё 10,
панель 16, билет внутри неё 10.

## Движение

Кривая одна: `--ease: cubic-bezier(.22, 1, .36, 1)`. Для перемещения по
экрану `--ease-io: cubic-bezier(.77, 0, .175, 1)`.

Длительность зависит от того, что происходит. Одну на всё не применяем:
смена цвета должна быть почти мгновенной, а появление секции читаемым.

| Токен | Время | Действие |
|---|---|---|
| `--d-color` | 120ms | наведение, смена цвета, усиление границы |
| `--d-press` | 140ms | отклик на нажатие, `scale(.97)` |
| `--d-lift` | 200ms | подъём карточки, сдвиг стрелки, смена состояния панели |
| `--d-open` | 280ms | выезд нижней панели, смена фона шапки |
| `--d-reveal` | 500ms | появление секции при скролле |

Анимируются только `transform`, `opacity` и `filter`.

Каждая анимация оправдана: появление секций держит иерархию чтения, зеркало
панели заявок объясняет механику, `scale(.97)` на кнопках даёт отклик,
стрелка сдвигается только у ссылок-переходов и никогда у кнопок отправки.
Бесконечных циклов нет, кроме ленты интеграций.

`prefers-reduced-motion: reduce` выключает перемещение и ленту, оставляя
мгновенные состояния. Ревилы устроены так, что без JS всё видно сразу.

## Вертикальный ритм

Одинаковый отступ между всеми секциями делает страницу монотонной, поэтому
ритм задан тремя разными величинами.

| Токен | Значение | Что разделяет |
|---|---|---|
| `--gap-block` | 88px телефон, 160px десктоп | соседние смысловые блоки |
| `--gap-head` | 32px телефон, 48px десктоп | заголовок секции и её содержимое |
| `--gap-group` | 24px | элементы внутри одной группы |

Секция берёт половину `--gap-block` на каждую сторону, поэтому между двумя
блоками получается ровно заявленная величина. Из общего правила выпадают
двое, и намеренно: лента интеграций прижата к герою сверху, а
секция-заявление получает воздуха на 44 процента больше остальных.

## Переносы строк

`text-wrap: balance` на всех заголовках, `text-wrap: pretty` на всех
абзацах. В русском тексте после предлогов и союзов из одной-двух букв, после
частых трёхбуквенных предлогов и внутри числовых групп стоят неразрывные
пробелы: 96 штук по странице. Ни один заголовок не заканчивается одиноким
словом на строке, это проверяется замером ширины последней строки на 320,
390 и 1440 пикселях.

## Мелочи, которые легко потерять при правках

- `::selection` белая полупрозрачная, а не сплошная заливка.
- Скроллбар перекрашен под тёмную тему, иначе системный светлый разрезает
  страницу по правому краю.
- `:focus-visible` это белая рамка 2px с отступом 2px, видимая на любом
  уровне глубины. `outline: none` без замены не ставить.
- `scroll-margin-top: 100px` на всех якорях: шапка липкая и без запаса
  накрывает заголовок секции.
- У всех SVG заданы `width` и `height` атрибутами, чтобы страница не
  прыгала до применения стилей.

## Запреты для этого проекта

- Никаких цветных элементов, включая градиенты и свечения в цвете.
- Никаких выдуманных кейсов, отзывов и цифр. Логотипы в ленте интеграций
  только официальные контуры Simple Icons, перерисовывать по памяти нельзя.
- Никаких длинных тире на странице.
- Никаких мелких заглавных подписей над каждой секцией.
- Никаких фейковых скриншотов интерфейса из `div`. Панель заявок настоящая:
  она отражает то, что посетитель прямо сейчас печатает в форме.
- Секция-заявление говорит о том, что мы умеем, а не о том, чего у нас нет.

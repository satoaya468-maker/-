<?php
/**
 * КРАСИВОЕ ЖЕЛЕЗО — настройки отправки заявок.
 *
 * ЭТОТ ФАЙЛ СОДЕРЖИТ СЕКРЕТЫ. Не коммитить реальный токен в репозиторий:
 * заполнять уже на хостинге. В git должен лежать config.sample.php.
 *
 * Как получить токен и chat_id — см. README-KRASIVOE-ZHELEZO.md, раздел
 * «Настройка Telegram-бота».
 */

declare(strict_types=1);

return [
    // --- Telegram -------------------------------------------------------
    // Токен от @BotFather, вид: 1234567890:AAF...
    'tg_token'   => getenv('KZ_TG_TOKEN') ?: 'ВСТАВЬТЕ_ТОКЕН_БОТА',

    // ID чата: свой ID из @userinfobot, либо -100... для группы/канала.
    'tg_chat_id' => getenv('KZ_TG_CHAT') ?: 'ВСТАВЬТЕ_CHAT_ID',

    // Username для кнопок «Отправить чертёж в Telegram», без «@».
    // ВАЖНО: это же значение продублируйте в двух местах, иначе кнопки поведут не туда:
    //   scripts/build-pages.py → TG_USER      (после правки запустить сборщик)
    //   assets/js/main.js      → CFG.tgUser
    'tg_username' => 'USERNAME',

    // --- Дублирование на почту (необязательно) --------------------------
    // Пустая строка — не дублировать.
    'email_to'   => 'stromilovm@mail.ru',
    'email_from' => 'zayavka@krasivoe-zhelezo.ru', // адрес на своём домене, иначе письмо уйдёт в спам

    // --- Файлы ----------------------------------------------------------
    'max_file_size'  => 20 * 1024 * 1024, // 20 МБ на файл
    'max_files'      => 5,
    'allowed_ext'    => ['jpg', 'jpeg', 'png', 'webp', 'pdf', 'dxf', 'dwg'],

    // --- Антиспам -------------------------------------------------------
    'rate_limit'        => 5,    // заявок с одного IP
    'rate_window'       => 600,  // за столько секунд
    'min_fill_seconds'  => 3,    // быстрее — это бот

    // --- Прочее ---------------------------------------------------------
    'site_name' => 'Красивое железо',
    'log_file'  => __DIR__ . '/logs/send.log', // папку logs создать и закрыть от чтения
];
